#include <dt-bindings/zmk/matrix_transform.h>

/{
    kscan0: kscan {
        compatible = "zmk,kscan-gpio-direct";
        wakeup-source;
        input-gpios
            = <&pro_micro 10 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>
            , <&pro_micro 24 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>
            ;
    };
}
